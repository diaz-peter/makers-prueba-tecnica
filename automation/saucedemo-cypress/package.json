{
  "name": "saucedemo-smoke-tests",
  "version": "1.0.0",
  "description": "Smoke tests de login para SauceDemo usando Cypress + Page Object Model",
  "scripts": {
    "test": "cypress run",
    "test:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.6.0"
  }
}
